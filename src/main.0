use core.types
use core.config
use core.session
use core.events
use core.policy
use core.agent
use tools.registry
use providers.registry

pub fun main(world: World) -> Void raises {
    let provider = defaultProvider()

    check world.out.write("ZERO Agent\n")
    check world.out.write("Provider: openrouter\n")
    check world.out.write("Model: anthropic/claude-sonnet-4\n")

    let tool = readFileTool()
    check world.out.write("Tool: read_file\n")

    check world.out.write("Agent ready\n")
}
