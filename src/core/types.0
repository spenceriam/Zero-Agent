pub enum InterfaceKind {
    Tui,
    Telegram,
    Scheduler,
}

pub enum RiskLevel {
    Safe,
    Mutating,
    Destructive,
    Blocked,
}

pub enum PermissionDecision {
    Allow,
    Ask,
    Deny,
}
