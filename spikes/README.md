# Phase 0 Spikes

Each spike must answer one question with evidence:

- Zero-native
- Rust bridge required
- deferred

Required spikes:

1. `zero-json-config`
2. `provider-http-call`
3. `provider-sse-streaming`
4. `tool-call-roundtrip`
5. `process-streaming`
6. `split-pane-terminal`
7. `telegram-polling`
8. `model-discovery`
9. `session-jsonl-store`
10. `zero-rust-bridge`

Each spike should include:

- purpose
- commands run
- result
- limitations
- recommendation
